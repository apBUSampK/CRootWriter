# CRootWriter
## .root format writer for COLA

Configurable parameters:
* `format` - `UniGen`, `AAMCC` and `COLA_native` (default) available. 
* `filename` - Output filename with the extension (i.e. `name.root`), obligatory.
* `write_coord` - Whether to write individual nuclei coordinates or not (default: False)